renderFlowersToPage(flowers);

const filterBtns = document.querySelector(".filters");

function sortingFn(event){
    console.log(event.target);
    isFinite(event.target.classList.contains("filter-btn"){
        //do something
        constfilterValue =event.target.getAttribute("data-filter");

        if(filterValue ==)
    })
}